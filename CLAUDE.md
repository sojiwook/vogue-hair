# SOGAM 플랫폼 — Claude Code 개발 지침

버전: 2026.05.23 | Claude Code가 이 프로젝트를 열 때 자동으로 읽는 파일
이 파일을 먼저 읽지 않고 코드 작성 금지


## 1. 프로젝트 정체성
소감(SOGAM)은 예방적 웰니스 데이터 플랫폼입니다.
헤어샵에서 두피 스캔 → 설문 → AI 분석 → 카카오 알림톡 리포트.

창업자: 소지욱 (비개발자, 헤어샵 10년 경력)
현재 단계: 파일럿 9명, 3개월 코호트 진행 중
다음 단계: B2B SaaS (전국 헤어샵 프랜차이즈)
개발 철학: 지금 필요한 것만 만든다. 증명되지 않은 기능에 시간 쓰지 않는다.

**핵심 미션 (모든 개발 결정의 기준)**

> "사람들이 일상에서 자신의 몸 상태를 자각하고, 이상 신호에 빠르게 반응하는 습관을 만든다."


## 2. 확정 기술 스택 (버전 명시)

| 항목 | 내용 | 주의사항 |
|------|------|---------|
| Frontend | React 19 + Vite 8 | React 19는 최신 버전, 일부 라이브러리 미호환 가능 |
| 서버 함수 | Vercel Serverless Functions (`api/` 폴더) | 민감 API 호출은 반드시 여기서 |
| 배포 | Vercel (vogue-hair 프로젝트) | main 브랜치 → 자동 배포 |
| 도메인 | sogam.kr (2026년 7월 전환 필수) | 현재 vogue-hair.vercel.app |
| DB | Supabase (PostgreSQL) | RLS 활성화 필수 |
| AI 분석 | Anthropic Claude API | api/analyze.js에서만 호출 |
| 알림톡 | SOLAPI → 카카오 알림톡 | 템플릿 변경 시 카카오 사전 심사 필요 |
| 리포트 | VITE_REPORT_BASE_URL 기반 페이지 | |

⚠️ 스택 변경 전 반드시 소지욱 대표에게 먼저 확인할 것
⚠️ 새 라이브러리 추가 전 React 19 호환 여부 반드시 확인

**서버/클라이언트 분리 원칙 (보안 핵심)**
```
api/ 폴더 (서버, 브라우저에 노출 안 됨)
├── analyze.js      ← Claude AI 분석 (ANTHROPIC_KEY 사용)
└── sendAlimtalk.js ← SOLAPI 카카오 알림톡 발송

src/ 폴더 (클라이언트, 브라우저에서 실행)
└── 민감 API 키 절대 사용 금지
```

**환경변수 목록 및 위치**
```
[서버 전용 - api/ 에서만 사용]
ANTHROPIC_KEY          ← Claude AI 분석
SOLAPI_API_KEY         ← 카카오 알림톡
SOLAPI_API_SECRET
SOLAPI_SENDER
SOLAPI_TEMPLATE_ID

[클라이언트 허용 - VITE_ 접두사]
VITE_SUPABASE_URL      ← DB 주소 (URL은 노출 가능)
VITE_REPORT_BASE_URL   ← 리포트 링크 기반 URL

[⚠️ 현재 보안 이슈 - 수정 필요]
VITE_SUPABASE_KEY      ← 브라우저 노출 중. api/ 함수로 이동 필요
```


## 3. 현재 시스템 흐름 (실제 작동 구조)
```
직원(스타일리스트) 로그인
        ↓
고객 정보 입력 + 설문 (수면/스트레스)
        ↓
두피 스캐너로 촬영 → 이미지 업로드
        ↓
api/analyze.js 호출
  → Anthropic Claude API로 이미지 분석
  → 두피 수치 5개 추출
        ↓
Supabase에 결과 저장
        ↓
SOLAPI → 카카오 알림톡 발송
  → 고객 리포트 링크 포함
        ↓
고객이 링크 클릭 → 리포트 페이지 확인
```


## 4. 핵심 데이터 모델 (임의 변경 금지)

**파일럿 고객 지표 (0~100 정수만 허용)**
```typescript
type PilotMetrics = {
  overall: number        // 종합점수 (0~100)
  sleepQuality: number   // 수면품질 (0~100)
  stress: number         // 스트레스 (0~100, 높을수록 나쁨)
  scalpMoisture: number  // 두피수분 (0~100)
  hairElasticity: number // 모발탄력 (0~100)
}

// 추후 추가 예정 (DB 설계 시 컬럼 미리 확보)
type SurveyExpansion = {
  exerciseFrequency?: number  // 운동빈도
  fatigue?: number            // 피로도
  alcoholFrequency?: number   // 음주빈도
  mealRegularity?: number     // 식사규칙성
}

type VisitStage = 'M0' | 'M1' | 'M2' | 'M3'
type PilotId = 'P01'|'P02'|'P03'|'P04'|'P05'|'P06'|'P07'|'P08'|'P09'
```

**경고 임계값**
```
stress >= 80        → 즉시 주의 (P06 박상우 사례)
sleepQuality <= 30  → 즉시 주의 (P02 권현구, P05 정영자 사례)
두 지표 동시 위험   → 최우선 알림 대상
```

**체크인 타이밍**
```
W2 체크인 = M0 시작일 + 14일  ← P08 임경철 현재 초과 중
M1 방문   = M0 시작일 + 30일
M3 완료   = M0 시작일 + 90일
```

**B2B 확장 대비 멀티테넌시**
지금은 1개 매장이지만, B2B 시 각 헤어샵 = 별도 테넌트.
Supabase 테이블 설계 시 `salon_id` 컬럼 미리 포함 권장.


## 5. 파일럿 데이터 보호 규칙 (최우선)
P01~P09 Supabase 데이터는 소감의 가장 중요한 자산입니다.

❌ 파일럿 데이터 삭제 금지 (실수 포함)
❌ 테스트 목적으로 실제 파일럿 테이블 사용 금지
❌ 마이그레이션 전 백업 없이 스키마 변경 금지

✅ 테스트는 별도 `test_` 접두사 테이블 사용
✅ 스키마 변경 전 반드시 소지욱 대표 확인
✅ 중요 변경 전 Supabase 백업 먼저


## 6. 보안 & 개인정보 (한국 개인정보보호법)

**절대 금지**
- 고객 실명을 코드/로그/주석에 하드코딩
- API 키, DB 접속 정보 코드에 직접 입력
- `console.log`에 고객 개인정보 출력
- `src/` (클라이언트)에서 `ANTHROPIC_KEY`, SOLAPI 키 사용
- `VITE_` 접두사를 민감한 키에 추가 사용

**고객 데이터 저장 원칙**
- 이름 대신 파일럿 ID (P01~P09) 사용
- 전화번호는 카카오 발송 후 로그에 남기지 않음
- Supabase RLS: 스타일리스트별 데이터 격리


## 7. 절대 금지 언어 (법적 리스크)
UI, 리포트, API 응답, 주석, 변수명 어디에도 사용 불가:

❌ 질병 조기 발견 / 진단 / 치료
❌ 암 예측 / 뇌 질환 / 의료적 판단
❌ ~을 치료합니다 / 병을 예방합니다
❌ 의학적으로 ~입니다

**허용 표현:**
✅ 웰니스 지표 / 생활습관 개선 / 건강 자각
✅ 자율신경 웰니스 지표
✅ 병원 방문을 권유합니다 (진단이 아님)
✅ 이상 신호가 감지됩니다 (의료 진단 아님)
✅ 건강 자각 습관 형성

의심스러우면 코드 작성 전 반드시 먼저 물어볼 것


## 8. 카카오 알림톡 제약사항
⚠️ 템플릿 내용 변경 = 카카오 사전 심사 필요 (수일 소요)
⚠️ 심사 없이 템플릿 수정하면 발송 중단됨
⚠️ `SOLAPI_TEMPLATE_ID`는 심사 완료된 템플릿 ID

템플릿 변경이 필요하면:
1. 소지욱 대표에게 먼저 알림
2. 새 템플릿 심사 신청 (기존 템플릿 유지)
3. 심사 완료 후 TEMPLATE_ID 교체


## 9. 에러 처리 기준

**SOLAPI 알림톡 발송 실패**
- 즉시 재시도 1회
- 재시도 실패 시 관리자(소지욱) 알림
- 고객에게 오류 절대 노출 금지
- 발송 실패 건 Supabase에 로그 저장

**두피 이미지 업로드 실패**
- 한국어 에러 메시지 표시
- 재시도 버튼 제공
- 빈 화면 / 흰 화면 절대 금지

**Claude AI 분석 타임아웃**
- 10초: 로딩 상태 유지
- 30초 초과: "잠시 후 다시 시도해주세요" 표시
- 분석 실패해도 두피 수치 입력값은 반드시 저장

**Supabase 저장 실패**
- 사용자에게 저장 실패 명확히 알림
- 데이터 소실 절대 금지 — 재시도 로직 필수


## 10. 크리티컬 데드라인
**2026년 7월 (약 60일 후)**
→ 보그헤어 가맹 계약 만료
→ 소감(SOGAM) 독립 브랜드 전환
→ sogam.kr 사이트 정식 오픈 필수
→ vogue-hair.vercel.app 에서 sogam.kr 로 도메인 전환

7월 전에 반드시 완료:
- [ ] sogam.kr Vercel 연결
- [ ] 브랜드 전환 UI 업데이트 (로고, 색상 #D9602B, 브랜드명)
- [ ] 기존 vogue-hair 링크 리다이렉트 처리


## 11. 코드 작성 원칙
- TypeScript `any` 사용 금지
- 함수 하나 = 역할 하나
- 로딩 상태 항상 처리
- 모든 사용자 메시지는 한국어
- 주석은 "무엇을"이 아니라 "왜"를 설명
- React 19 새 기능 사용 전 호환성 확인

**새 기능 만들기 전 체크리스트**
- [ ] 지금 파일럿 9명에게 실제로 필요한가?
- [ ] 더 단순한 방법은 없는가?
- [ ] B2B 확장(salon_id 멀티테넌시) 시 이 구조가 버티는가?
- [ ] 보안/개인정보 문제는 없는가?
- [ ] 파일럿 데이터가 안전한가?
- [ ] 7월 브랜드 전환에 영향을 주는가?


## 12. 지금 만들지 말아야 할 것
❌ 고객 자체 로그인 / 회원가입 (직원만 사용)
❌ 결제 시스템
❌ 네이티브 앱 (PWA로 충분한 이유: 설치 없이 작동)
❌ 복잡한 관리자 대시보드
❌ 실시간 알림 (WebSocket)
❌ 다국어 지원
❌ SOLAPI 대체 다른 메시징 시스템


## 13. 현재 알려진 기술 부채
```
[HIGH] VITE_SUPABASE_KEY 브라우저 노출
  → api/ 서버 함수로 Supabase 접근 이전 필요
  → Survey.jsx: api/submit-survey.js 이전 후 anon_insert 정책 삭제
  → App.jsx:    api/save-scalp.js 이전 후 anon_insert 정책 삭제

[MEDIUM] sogam.kr 도메인 미연결
  → 7월 전 Vercel Custom Domain 설정 필요
```

**2026-05-24 RLS 정리 완료 내역**
개발 초기에 "일단 되게 하자"식으로 추가된 anon/public 과잉 허용 정책 21개 중
위험 정책 18개 삭제 완료. 아래 4개만 Survey.jsx 의존으로 보류 중:
- customers: anon_insert_customers
- visits:    anon_insert_visits
- surveys:   anon_insert_surveys
→ api/submit-survey.js, api/save-scalp.js 이전 완료 후 이 4개도 삭제할 것.


## 14. 설명 방식 (비개발자 창업자 대상)
소지욱 대표는 비개발자입니다:

- 코드 설명 → 헤어샵/사업 비유 우선
- 선택지 → "A하면 장단점, B하면 장단점" 형식
- 기술 용어 → 괄호 안에 한 줄 설명 추가
- 작업 완료 → "뭘 만들었고, 어떻게 테스트하는지" 항상 안내
- 에러 발생 → 원인과 해결책을 비개발자 언어로 설명


---
버전: 2026.05.23 | 소감(SOGAM) 내부 개발 문서 | 외부 공개 금지
